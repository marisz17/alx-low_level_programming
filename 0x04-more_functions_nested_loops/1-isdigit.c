#include "main.h"

/**
 * _isdigit - checks for a digit
 * @c: number
 * Return: 1 for upper, 0 for other
 */
int _isdigit(int c)
{
	if (c >= 48 && c < 58)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
